<h1>Chat #<%= @chat.id %></h1>

<p>Remitente: <%= "#{@chat.sender.first_name} #{@chat.sender.last_name}" %></p>
<p>Receptor: <%= "#{@chat.receiver.first_name} #{@chat.receiver.last_name}" %></p>


<h2>Mensajes</h2>
<ul>
  <% @chat.messages.each do |msg| %>
    <li><%= "#{msg.user.first_name} #{msg.user.last_name}" %>: <%= msg.body %></li>
  <% end %>
</ul>

<%= link_to 'Editar', edit_chat_path(@chat) %>
